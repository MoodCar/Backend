<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h2><%= title %></h2>
        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th>email</th>
                    <th>name</th>
                    <th>provider</th>
                    <th>providerId</th>
                    <th>token</th>
                    <th>auth</th>
                    <th>location</th>
                    <th>preference</th>
                    <th>reg_date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for (var i =0;i < users.length; i=i+1){ %>
                    <tr>
                        <td><%= users[i].email %></td>
                        <td><%= users[i].name %></td>
                        <td><%= users[i].provider %></td>
                        <td><%= users[i].providerId %></td>
                        <td><%= users[i].token %></td>
                        <td><%= users[i].auth %></td>
                        <td><%= users[i].location %></td>
                        <td><%= users[i].preference %></td>
                        <td><%= users[i].reg_date %></td>
                        <td>
                            <a href="/admin/users/details/<%= users[i].providerId %>" class="btn btn-sm btn-danger">삭제</a>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        <a href="/admin" class="btn btn-sm btn-primary">관리자 페이지</a><br/>
    </div>
</body>
</html>